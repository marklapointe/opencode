# desktop

Path: /Users/mlapointe/git/opencode/packages/desktop/src
Files: 26

## File Tree

- `bindings.ts` → const commands = { (67 ln)
  (in src/)
- `cli.ts` → async function installCli(): Promise<void> { (43 ln)
  (in src/)
- `entry.tsx` → (no symbol detected) (5 ln)
  (in src/)
- `env.d.ts` → (no symbol detected) (9 ln)
  (in src/)
  - `ar.ts` → const dict = { (59 ln)
    (in src/i18n/)
  - `br.ts` → const dict = { (61 ln)
    (in src/i18n/)
  - `bs.ts` → const dict = { (62 ln)
    (in src/i18n/)
  - `da.ts` → const dict = { (61 ln)
    (in src/i18n/)
  - `de.ts` → const dict = { (62 ln)
    (in src/i18n/)
  - `en.ts` → const dict = { (61 ln)
    (in src/i18n/)
  - `es.ts` → const dict = { (61 ln)
    (in src/i18n/)
  - `fr.ts` → const dict = { (62 ln)
    (in src/i18n/)
  - `index.ts` → type Locale = (192 ln)
    (in src/i18n/)
  - `ja.ts` → const dict = { (62 ln)
    (in src/i18n/)
  - `ko.ts` → const dict = { (60 ln)
    (in src/i18n/)
  - `no.ts` → const dict = { (61 ln)
    (in src/i18n/)
  - `pl.ts` → const dict = { (62 ln)
    (in src/i18n/)
  - `ru.ts` → const dict = { (61 ln)
    (in src/i18n/)
  - `zh.ts` → const dict = { (59 ln)
    (in src/i18n/)
  - `zht.ts` → const dict = { (59 ln)
    (in src/i18n/)
- `index.tsx` → function handleClick(e: MouseEvent) { (505 ln)
  (in src/)
- `loading.tsx` → (no symbol detected) (90 ln)
  (in src/)
- `menu.ts` → async function createMenu(trigger: (id: string) => void) { (190 ln)
  (in src/)
- `styles.css` → (no symbol detected) (7 ln)
  (in src/)
- `updater.ts` → const UPDATER_ENABLED = window.__OPENCODE__?.updaterEnabled ?? false (51 ln)
  (in src/)
- `webview-zoom.ts` → { webviewZoom } (37 ln)
  (in src/)

Auto-generated: 2026-05-03
