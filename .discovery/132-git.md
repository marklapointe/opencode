132-git.md

Directory: src/git/

Overview: Git integration wrapper using a cross-spawn spawner to execute git commands and surface repository state.

- index.ts
  - Exports: Git.Service, Git layer, and re-export pattern export * as Git from "."
  - Role: Provides a DI-wired Git service with run, branch, prefix, defaultBranch, hasHead, mergeBase, show, status, diff, stats
- The file demonstrates a comprehensive wrapper around Git CLI, with configured git options, and a consistent schema for results.

- Other files (if present) provide specific helpers and utilities; this module acts as the central Git integration point.

End of 132-git.md
