# Mapping Group 035
Files: 14

## File Tree
**packages/desktop-electron/src/**
  - `html.test.ts` →  (62 ln)  [typescript]
    → Packaged Electron windows load renderer HTML via the privileged `oc://` protocol. Root-relative asset paths like `src="/foo.js"` would resolve from th
  - `ar.ts` → dict (26 ln)  [typescript]
    → export const dict = {
  - `br.ts` → dict (27 ln)  [typescript]
    → export const dict = {
  - `bs.ts` → dict (28 ln)  [typescript]
    → export const dict = {
  - `da.ts` → dict (28 ln)  [typescript]
    → export const dict = {
  - `de.ts` → dict (28 ln)  [typescript]
    → export const dict = {
  - `en.ts` → dict (27 ln)  [typescript]
    → export const dict = {
  - `es.ts` → dict (27 ln)  [typescript]
    → export const dict = {
  - `fr.ts` → dict (28 ln)  [typescript]
    → export const dict = {
  - `index.ts` → t (188 ln)  [typescript]
    → Imports: ./en
  - `ja.ts` → dict (28 ln)  [typescript]
    → export const dict = {
  - `ko.ts` → dict (27 ln)  [typescript]
    → export const dict = {
  - `no.ts` → dict (28 ln)  [typescript]
    → export const dict = {
  - `pl.ts` → dict (28 ln)  [typescript]
    → export const dict = {

---
Generated: 2026-05-03 15:01:00
