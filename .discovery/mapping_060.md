# Mapping Group 060
Files: 14

## File Tree
**packages/opencode/src/**
  - `public.ts` → PublicApi (505 ln)  [typescript]
    → Fix component schemas that are self-referencing `$ref`s — an Effect OpenAPI generation bug where annotated union arms that share AST nodes with other
  - `server.ts` → createRoutes (198 ln)  [typescript]
    → Imports: effect
  - `index.ts` → InstanceRoutes (278 ln)  [typescript]
    → Imports: hono-openapi
  - `mcp.ts` → McpRoutes (277 ln)  [typescript]
    → Imports: hono
  - `middleware.ts` → InstanceMiddleware (35 ln)  [typescript]
    → Imports: hono
  - `permission.ts` → PermissionRoutes (73 ln)  [typescript]
    → Imports: hono
  - `project.ts` → ProjectRoutes (122 ln)  [typescript]
    → Imports: hono
  - `provider.ts` → ProviderRoutes (158 ln)  [typescript]
    → Imports: hono
  - `pty.ts` → PtyRoutes (276 ln)  [typescript]
    → Imports: hono
  - `question.ts` → QuestionRoutes (111 ln)  [typescript]
    → Imports: hono
  - `session.ts` → SessionRoutes (1124 ln)  [typescript]
    → Imports: hono
  - `sync.ts` → SyncRoutes (152 ln)  [typescript]
    → Imports: zod
  - `trace.ts` → paramToAttributeKey (59 ln)  [typescript]
    → Imports: hono
  - `tui.ts` → nextTuiRequest (399 ln)  [typescript]
    → Imports: hono

---
Generated: 2026-05-03 15:01:00
