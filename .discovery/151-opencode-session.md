# opencode-session

Path: /Users/mlapointe/git/opencode/packages/opencode/src/session
Files: 19

## File Tree

- `compaction.ts` → const Event = { (630 ln)
  (in session/)
- `instruction.ts` → interface Interface { (232 ln)
  (in session/)
- `llm.ts` → const OUTPUT_TOKEN_MAX = ProviderTransform.OUTPUT_TOKEN_MAX (469 ln)
  (in session/)
- `message-v2.ts` → const SYNTHETIC_ATTACHMENT_PROMPT = "Attached image(s) from tool result:" (1221 ln)
  (in session/)
- `message.ts` → const OutputLengthError = namedSchemaError("MessageOutputLengthError", {}) (192 ln)
  (in session/)
- `overflow.ts` → function usable(input: { cfg: Config.Info; model: Provider.Model }) { (26 ln)
  (in session/)
- `processor.ts` → type Result = "compact" | "stop" | "continue" (619 ln)
  (in session/)
- `projectors.ts` → type DeepPartial<T> = T extends object ? { [K in keyof T]?: DeepPartial<T[K]> |  (139 ln)
  (in session/)
- `prompt.ts` → interface Interface { (1784 ln)
  (in session/)
- `retry.ts` → type Err = ReturnType<NamedError["toObject"]> (125 ln)
  (in session/)
- `revert.ts` → const RevertInput = Schema.Struct({ (164 ln)
  (in session/)
- `run-state.ts` → interface Interface { (110 ln)
  (in session/)
- `schema.ts` → const SessionID = Schema.String.annotate({ [ZodOverride]: Identifier.schema("ses (35 ln)
  (in session/)
- `session.sql.ts` → const SessionTable = sqliteTable( (124 ln)
  (in session/)
- `session.ts` → function isDefaultTitle(title: string) { (902 ln)
  (in session/)
- `status.ts` → const Info = Schema.Union([ (88 ln)
  (in session/)
- `summary.ts` → interface Interface { (165 ln)
  (in session/)
- `system.ts` → function provider(model: Provider.Model) { (84 ln)
  (in session/)
- `todo.ts` → const Info = Schema.Struct({ (86 ln)
  (in session/)

Auto-generated: 2026-05-03
