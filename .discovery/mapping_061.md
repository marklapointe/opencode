# Mapping Group 061
Files: 14

**packages/opencode/parsers-config.ts/**
- `parsers-config.ts` →  (290 ln)  [config]

**packages/opencode/script/**
- `build-node.ts` →  (62 ln)  [typescript]
  → Imports: @opencode-ai/script
  → imports: @opencode-ai/script, fs, path
- `build.ts` →  (267 ln)  [typescript]
  → Imports: bun
  → imports: bun, fs, path
- `check-migrations.ts` →  (16 ln)  [typescript]
  → Imports: bun
  → imports: bun
- `fix-node-pty.ts` →  (28 ln)  [typescript]
  → Imports: fs/promises
  → imports: fs/promises, path, url
- `generate.ts` →  (23 ln)  [typescript]
  → Imports: path
  → imports: path, url
- `publish.ts` →  (196 ln)  [typescript]
  → Imports: bun
  → imports: bun, ../package.json, @opencode-ai/script
- `schema.ts` →  (63 ln)  [typescript]
  → We'll use the `default` values of the field as the only value in `examples`. This will ensure no docs are needed to be read, as the configuration is self-documenting.
  → imports: zod, @/config/config, ../src/cli/cmd/tui/config/tui
- `time.ts` →  (6 ln)  [typescript]
  → Imports: path
  → imports: path
- `trace-imports.ts` →  (153 ln)  [typescript]
  → const BASE_DIR = "/home/thdxr/dev/projects/anomalyco/opencode/packages/opencode"
- `upgrade-opentui.ts` →  (64 ln)  [typescript]
  → Imports: node:path
  → imports: node:path

**packages/opencode/scripts/**
- `diff-sdk-types.sh` →  (52 ln)  [bash]

**packages/opencode/specs/**
- `facades.md` →  (221 ln)  [docs]
- `http-api.md` →  (401 ln)  [docs]

---
Generated: 2026-05-04 02:45:04Z
