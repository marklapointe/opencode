Root: plugin/tsconfig.json

ASCII Tree:

├── [export] JSON config for TypeScript project

Description
- TS configuration for the plugin package (module: ESNext, etc.).

Data Flow
- Used by TypeScript compiler during build/typechecking.

Side Effects
- None.
