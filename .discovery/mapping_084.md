# Mapping Group 084
Files: 14

## File Tree
**packages/enterprise/test/**
  - `storage.test.ts` →  (64 ln)  [typescript]
    → Imports: bun:test

**packages/enterprise/vite.config.ts/**
  - `vite.config.ts` →  (36 ln)  [typescript]
    → Imports: vite

**packages/function/src/**
  - `api.ts` → Env (388 ln)  [typescript]
    → Used by the GitHub action to get GitHub installation access token given the OIDC token /

**packages/function/sst-env.d.ts/**
  - `sst-env.d.ts` →  (299 ln)  [typescript]

**packages/opencode/drizzle.config.ts/**
  - `drizzle.config.ts` →  (10 ln)  [typescript]
    → Imports: drizzle-kit

**packages/opencode/parsers-config.ts/**
  - `parsers-config.ts` →  (290 ln)  [typescript]

**packages/opencode/script/**
  - `build-node.ts` →  (62 ln)  [typescript]
    → Imports: @opencode-ai/script
  - `build.ts` → binaries (267 ln)  [typescript]
    → Imports: bun
  - `check-migrations.ts` →  (16 ln)  [typescript]
    → Imports: bun
  - `fix-node-pty.ts` →  (28 ln)  [typescript]
    → Imports: fs/promises
  - `generate.ts` →  (23 ln)  [typescript]
    → Imports: path
  - `publish.ts` →  (196 ln)  [typescript]
    → Imports: bun
  - `schema.ts` →  (63 ln)  [typescript]
    → We'll use the `default` values of the field as the only value in `examples`. This will ensure no docs are needed to be read, as the configuration is s
  - `time.ts` →  (6 ln)  [typescript]
    → Imports: path

---
Generated: 2026-05-03 15:01:00
