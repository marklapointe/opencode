# Mapping Group 094
Files: 14

**packages/opencode/src/**
- `event.sql.ts` → EventSequenceTable (16 ln)  [typescript]
  → Imports: drizzle-orm/sqlite-core
  → imports: drizzle-orm/sqlite-core
- `index.ts` → reset (387 ln)  [typescript]
  → Imports: zod
  → imports: zod, @/storage/db, drizzle-orm
- `schema.ts` → EventID (13 ln)  [typescript]
  → Imports: effect
  → imports: effect, @/id/id, @/util/effect-zod
- `temporary.ts` →  (33 ln)  [typescript]
  → Imports: yargs
  → imports: yargs, ./cli/cmd/tui/thread, @opencode-ai/core/installation/version
- `apply_patch.ts` → Parameters (309 ln)  [typescript]
  → Imports: effect
  → imports: effect, ../bus, ../file/watcher
- `bash.ts` → Parameters (635 ln)  [typescript]
  → Imports: effect
  → imports: effect, @/util/schema, os
- `edit.ts` → trimDiff (711 ln)  [typescript]
  → Levenshtein distance algorithm implementation /
  → imports: effect, @/lsp/lsp, diff
- `external-directory.ts` → assertExternalDirectory (49 ln)  [typescript]
  → Imports: path
  → imports: path, effect, @/effect/instance-state
- `glob.ts` → Parameters (97 ln)  [typescript]
  → Imports: path
  → imports: path, effect, @/effect/instance-state
- `grep.ts` → Parameters (151 ln)  [typescript]
  → Imports: path
  → imports: path, effect, effect
- `invalid.ts` → Parameters (21 ln)  [typescript]
  → Imports: effect
  → imports: effect
- `lsp.ts` → Parameters (113 ln)  [typescript]
  → Imports: effect
  → imports: effect, path, @/lsp/lsp
- `mcp-exa.ts` → SearchArgs (73 ln)  [typescript]
  → Imports: effect
  → imports: effect, effect/unstable/http
- `plan.ts` → Parameters (82 ln)  [typescript]
  → Imports: path
  → imports: path, effect, ../question

---
Generated: 2026-05-04 02:45:04Z
