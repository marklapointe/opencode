# Mapping Group 136
Files: 14

**packages/ui/src/**
- `no.ts` → dict (162 ln)  [typescript]
  → Imports: ./en
  → imports: ./en
- `pl.ts` → dict (158 ln)  [typescript]
  → export const dict = {
- `ru.ts` → dict (158 ln)  [typescript]
  → export const dict = {
- `th.ts` → dict (160 ln)  [typescript]
  → export const dict = {
- `tr.ts` → dict (165 ln)  [typescript]
  → Imports: ./en
  → imports: ./en
- `zh.ts` → dict (163 ln)  [typescript]
  → Imports: ./en
  → imports: ./en
- `zht.ts` → dict (163 ln)  [typescript]
  → Imports: ./en
  → imports: ./en
- `comment-hover.ts` → createHoverCommentUtility (74 ln)  [typescript]
  → export function createHoverCommentUtility(props: {
- `commented-lines.ts` → markCommentedDiffLines (91 ln)  [typescript]
  → Imports: @pierre/diffs
  → imports: @pierre/diffs, ./diff-selection
- `diff-selection.ts` → findDiffSide (71 ln)  [typescript]
  → Imports: @pierre/diffs
  → imports: @pierre/diffs
- `file-find.ts` → createFileFind (485 ln)  [typescript]
  → Imports: solid-js
  → imports: solid-js, @solid-primitives/event-listener, @solid-primitives/resize-observer
- `file-runtime.ts` → createReadyWatcher (114 ln)  [typescript]
  → export function createReadyWatcher(): ReadyWatcher {
- `file-selection.ts` → findElement (85 ln)  [typescript]
  → Imports: @pierre/diffs
  → imports: @pierre/diffs, ./selection-bridge
- `index.ts` → createDefaultOptions (191 ln)  [typescript]
  → Imports: @pierre/diffs
  → imports: @pierre/diffs, solid-js, ../components/line-comment-styles

---
Generated: 2026-05-04 02:45:04Z
