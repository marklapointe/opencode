# opencode-server

Path: /Users/mlapointe/git/opencode/packages/opencode/src/server
Files: 77

## File Tree

- `adapter.bun.ts` → const adapter: Adapter = { (44 ln)
  (in server/)
- `adapter.node.ts` → const adapter: Adapter = { (73 ln)
  (in server/)
- `adapter.ts` → type FetchApp = { (26 ln)
  (in server/)
- `backend.ts` → type Backend = "effect-httpapi" | "hono" (32 ln)
  (in server/)
- `cors.ts` → type CorsOptions = { readonly cors?: ReadonlyArray<string> } (14 ln)
  (in server/)
- `error.ts` → const ERRORS = { (36 ln)
  (in server/)
- `event.ts` → const Event = { (7 ln)
  (in server/)
- `fence.ts` → function load(ids?: string[]) { (90 ln)
  (in server/)
- `mdns.ts` → function publish(port: number, domain?: string) { (60 ln)
  (in server/)
- `middleware.ts` → const ErrorMiddleware: ErrorHandler = (err, c) => { (86 ln)
  (in server/)
- `projectors.ts` → function initProjectors() { (28 ln)
  (in server/)
- `proxy-util.ts` → function headers(input: Request | HeadersInit | Record<string, string>, extra?:  (48 ln)
  (in server/)
- `proxy.ts` → function httpEffect(url: string | URL, extra: HeadersInit | undefined, req: Requ (149 ln)
  (in server/)
    - `index.ts` → function ControlPlaneRoutes(): Hono { (160 ln)
      (in server/routes/control/)
    - `workspace.ts` → const WorkspaceRoutes = lazy(() => (210 ln)
      (in server/routes/control/)
  - `global.ts` → const GlobalDisposedEvent = BusEvent.define("global.disposed", Schema.Struct({}) (287 ln)
    (in server/routes/)
    - `config.ts` → const ConfigRoutes = lazy(() => (89 ln)
      (in server/routes/instance/)
    - `event.ts` → const EventRoutes = () => (88 ln)
      (in server/routes/instance/)
    - `experimental.ts` → const ExperimentalRoutes = lazy(() => (419 ln)
      (in server/routes/instance/)
    - `file.ts` → const FileRoutes = lazy(() => (190 ln)
      (in server/routes/instance/)
      - `api.ts` → const RootHttpApi = HttpApi.make("opencode-root").addHttpApi(ControlApi).addHttp (54 ln)
        (in server/routes/instance/httpapi/)
      - `event.ts` → const EventPaths = { (77 ln)
        (in server/routes/instance/httpapi/)
        - `config.ts` → const ConfigApi = HttpApi.make("config") (61 ln)
          (in server/routes/instance/httpapi/groups/)
        - `control.ts` → const LogInput = Schema.Struct({ (75 ln)
          (in server/routes/instance/httpapi/groups/)
        - `experimental.ts` → const ConsoleSwitchPayload = Schema.Struct({ (214 ln)
          (in server/routes/instance/httpapi/groups/)
        - `file.ts` → const FileQuery = Schema.Struct({ (121 ln)
          (in server/routes/instance/httpapi/groups/)
        - `global.ts` → const GlobalUpgradeInput = Schema.Struct({ (106 ln)
          (in server/routes/instance/httpapi/groups/)
        - `instance.ts` → const VcsDiffQuery = Schema.Struct({ (143 ln)
          (in server/routes/instance/httpapi/groups/)
        - `mcp.ts` → const AddPayload = Schema.Struct({ (145 ln)
          (in server/routes/instance/httpapi/groups/)
        - `metadata.ts` → function described<S extends Schema.Top>(schema: S, description: string): S { (18 ln)
          (in server/routes/instance/httpapi/groups/)
        - `permission.ts` → const PermissionApi = HttpApi.make("permission") (58 ln)
          (in server/routes/instance/httpapi/groups/)
        - `project.ts` → const ProjectApi = HttpApi.make("project") (77 ln)
          (in server/routes/instance/httpapi/groups/)
        - `provider.ts` → const ProviderApi = HttpApi.make("provider") (76 ln)
          (in server/routes/instance/httpapi/groups/)
        - `pty.ts` → const Params = Schema.Struct({ ptyID: PtyID }) (127 ln)
          (in server/routes/instance/httpapi/groups/)
        - `question.ts` → const QuestionApi = HttpApi.make("question") (70 ln)
          (in server/routes/instance/httpapi/groups/)
        - `session.ts` → const ListQuery = Schema.Struct({ (429 ln)
          (in server/routes/instance/httpapi/groups/)
        - `sync.ts` → const ReplayEvent = Schema.Struct({ (92 ln)
          (in server/routes/instance/httpapi/groups/)
        - `tui.ts` → const CommandPayload = Schema.Struct({ command: Schema.String }) (197 ln)
          (in server/routes/instance/httpapi/groups/)
        - `workspace.ts` → const CreatePayload = Schema.Struct(Struct.omit(Workspace.CreateInput.fields, [" (103 ln)
          (in server/routes/instance/httpapi/groups/)
        - `config.ts` → const configHandlers = HttpApiBuilder.group(InstanceHttpApi, "config", (handlers (34 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `control.ts` → const controlHandlers = HttpApiBuilder.group(RootHttpApi, "control", (handlers)  (34 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `experimental.ts` → const experimentalHandlers = HttpApiBuilder.group(InstanceHttpApi, "experimental (155 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `file.ts` → const fileHandlers = HttpApiBuilder.group(InstanceHttpApi, "file", (handlers) => (54 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `global.ts` → const globalHandlers = HttpApiBuilder.group(RootHttpApi, "global", (handlers) => (156 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `instance.ts` → const instanceHandlers = HttpApiBuilder.group(InstanceHttpApi, "instance", (hand (79 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `mcp.ts` → const mcpHandlers = HttpApiBuilder.group(InstanceHttpApi, "mcp", (handlers) => (68 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `permission.ts` → const permissionHandlers = HttpApiBuilder.group(InstanceHttpApi, "permission", ( (29 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `project.ts` → const projectHandlers = HttpApiBuilder.group(InstanceHttpApi, "project", (handle (46 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `provider.ts` → const providerHandlers = HttpApiBuilder.group(InstanceHttpApi, "provider", (hand (89 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `pty.ts` → const ptyHandlers = HttpApiBuilder.group(InstanceHttpApi, "pty", (handlers) => (121 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `question.ts` → const questionHandlers = HttpApiBuilder.group(InstanceHttpApi, "question", (hand (33 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `session.ts` → const sessionHandlers = HttpApiBuilder.group(InstanceHttpApi, "session", (handle (388 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `sync.ts` → const syncHandlers = HttpApiBuilder.group(InstanceHttpApi, "sync", (handlers) => (77 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `tui.ts` → const tuiHandlers = HttpApiBuilder.group(InstanceHttpApi, "tui", (handlers) => (135 ln)
          (in server/routes/instance/httpapi/handlers/)
        - `workspace.ts` → const workspaceHandlers = HttpApiBuilder.group(InstanceHttpApi, "workspace", (ha (61 ln)
          (in server/routes/instance/httpapi/handlers/)
      - `lifecycle.ts` → const markInstanceForDisposal = (ctx: InstanceContext) => (63 ln)
        (in server/routes/instance/httpapi/)
        - `authorization.ts` → class Authorization extends HttpApiMiddleware.Service<Authorization>()( (128 ln)
          (in server/routes/instance/httpapi/middleware/)
        - `instance-context.ts` → class InstanceContextMiddleware extends HttpApiMiddleware.Service< (55 ln)
          (in server/routes/instance/httpapi/middleware/)
        - `proxy.ts` → function websocket( (86 ln)
          (in server/routes/instance/httpapi/middleware/)
        - `workspace-routing.ts` → class WorkspaceRouteContext extends Context.Service< (228 ln)
          (in server/routes/instance/httpapi/middleware/)
      - `public.ts` → const PublicApi = OpenCodeHttpApi.annotateMerge( (505 ln)
        (in server/routes/instance/httpapi/)
      - `server.ts` → const context = Context.makeUnsafe<unknown>(new Map()) (198 ln)
        (in server/routes/instance/httpapi/)
    - `index.ts` → const InstanceRoutes = (upgrade: UpgradeWebSocket): Hono => { (278 ln)
      (in server/routes/instance/)
    - `mcp.ts` → const McpRoutes = lazy(() => (277 ln)
      (in server/routes/instance/)
    - `middleware.ts` → function InstanceMiddleware(workspaceID?: WorkspaceID): MiddlewareHandler { (35 ln)
      (in server/routes/instance/)
    - `permission.ts` → const PermissionRoutes = lazy(() => (73 ln)
      (in server/routes/instance/)
    - `project.ts` → const ProjectRoutes = lazy(() => (122 ln)
      (in server/routes/instance/)
    - `provider.ts` → const ProviderRoutes = lazy(() => (158 ln)
      (in server/routes/instance/)
    - `pty.ts` → function PtyRoutes(upgradeWebSocket: UpgradeWebSocket) { (276 ln)
      (in server/routes/instance/)
    - `question.ts` → const QuestionRoutes = lazy(() => (111 ln)
      (in server/routes/instance/)
    - `session.ts` → const SessionRoutes = lazy(() => (1124 ln)
      (in server/routes/instance/)
    - `sync.ts` → const SyncRoutes = lazy(() => (152 ln)
      (in server/routes/instance/)
    - `trace.ts` → interface RequestLike { (59 ln)
      (in server/routes/instance/)
    - `tui.ts` → const TuiRequest = z.object({ (399 ln)
      (in server/routes/instance/)
  - `ui.ts` → async function serveUI(request: Request) { (127 ln)
    (in server/routes/)
- `server.ts` → type Listener = { (195 ln)
  (in server/)
- `workspace.ts` → function isLocalWorkspaceRoute(method: string, path: string) { (130 ln)
  (in server/)

Auto-generated: 2026-05-03
