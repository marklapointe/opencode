# desktop-electron

Path: /Users/mlapointe/git/opencode/packages/desktop-electron/src
Files: 40

## File Tree

  - `apps.ts` → function checkAppExists(appName: string): boolean { (148 ln)
    (in src/main/)
  - `constants.ts` → const CHANNEL: Channel = raw === "dev" || raw === "beta" || raw === "prod" ? raw (10 ln)
    (in src/main/)
  - `env.d.ts` → (no symbol detected) (29 ln)
    (in src/main/)
  - `index.ts` → function setupApp() { (452 ln)
    (in src/main/)
  - `ipc.ts` → function registerIpcHandlers(deps: Deps) { (204 ln)
    (in src/main/)
  - `logging.ts` → function initLogging() { (40 ln)
    (in src/main/)
  - `markdown.ts` → function parseMarkdown(input: string) { (16 ln)
    (in src/main/)
  - `menu.ts` → function createMenu(deps: Deps) { (136 ln)
    (in src/main/)
  - `migrate.ts` → function migrate() { (91 ln)
    (in src/main/)
  - `server.ts` → type WslConfig = { enabled: boolean } (101 ln)
    (in src/main/)
  - `shell-env.test.ts` → (no symbol detected) (43 ln)
    (in src/main/)
  - `shell-env.ts` → function getUserShell() { (88 ln)
    (in src/main/)
  - `store.ts` → function getStore(name = SETTINGS_STORE) { (17 ln)
    (in src/main/)
  - `windows.ts` → function setBackgroundColor(color: string) { (206 ln)
    (in src/main/)
  - `index.ts` → (no symbol detected) (71 ln)
    (in src/preload/)
  - `types.ts` → type InitStep = { phase: "server_waiting" } | { phase: "sqlite_waiting" } | { ph (79 ln)
    (in src/preload/)
  - `cli.ts` → async function installCli(): Promise<void> { (12 ln)
    (in src/renderer/)
  - `env.d.ts` → (no symbol detected) (10 ln)
    (in src/renderer/)
  - `html.test.ts` → (no symbol detected) (62 ln)
    (in src/renderer/)
    - `ar.ts` → const dict = { (26 ln)
      (in src/renderer/i18n/)
    - `br.ts` → const dict = { (27 ln)
      (in src/renderer/i18n/)
    - `bs.ts` → const dict = { (28 ln)
      (in src/renderer/i18n/)
    - `da.ts` → const dict = { (28 ln)
      (in src/renderer/i18n/)
    - `de.ts` → const dict = { (28 ln)
      (in src/renderer/i18n/)
    - `en.ts` → const dict = { (27 ln)
      (in src/renderer/i18n/)
    - `es.ts` → const dict = { (27 ln)
      (in src/renderer/i18n/)
    - `fr.ts` → const dict = { (28 ln)
      (in src/renderer/i18n/)
    - `index.ts` → type Locale = (188 ln)
      (in src/renderer/i18n/)
    - `ja.ts` → const dict = { (28 ln)
      (in src/renderer/i18n/)
    - `ko.ts` → const dict = { (27 ln)
      (in src/renderer/i18n/)
    - `no.ts` → const dict = { (28 ln)
      (in src/renderer/i18n/)
    - `pl.ts` → const dict = { (28 ln)
      (in src/renderer/i18n/)
    - `ru.ts` → const dict = { (27 ln)
      (in src/renderer/i18n/)
    - `zh.ts` → const dict = { (26 ln)
      (in src/renderer/i18n/)
    - `zht.ts` → const dict = { (26 ln)
      (in src/renderer/i18n/)
  - `index.tsx` → function handleClick(e: MouseEvent) { (377 ln)
    (in src/renderer/)
  - `loading.tsx` → (no symbol detected) (83 ln)
    (in src/renderer/)
  - `styles.css` → (no symbol detected) (0 ln)
    (in src/renderer/)
  - `updater.ts` → async function runUpdater({ alertOnFail }: { alertOnFail: boolean }) { (12 ln)
    (in src/renderer/)
  - `webview-zoom.ts` → { webviewZoom } (38 ln)
    (in src/renderer/)

Auto-generated: 2026-05-03
