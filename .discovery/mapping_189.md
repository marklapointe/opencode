# Mapping Group 189
Files: 14

**script/beta.ts/**
- `beta.ts` →  (360 ln)  [typescript]
  → Imports: bun
  → imports: bun, fs/promises

**script/changelog.ts/**
- `changelog.ts` →  (76 ln)  [typescript]
  → Imports: fs/promises
  → imports: fs/promises, path, util

**script/duplicate-pr.ts/**
- `duplicate-pr.ts` →  (79 ln)  [typescript]
  → Imports: path
  → imports: path, bun, @opencode-ai/sdk

**script/format.ts/**
- `format.ts` →  (5 ln)  [typescript]
  → Imports: bun
  → imports: bun

**script/generate.ts/**
- `generate.ts` →  (9 ln)  [typescript]
  → Imports: bun
  → imports: bun

**script/github/close-issues.ts/**
- `close-issues.ts` →  (96 ln)  [typescript]
  → const repo = "anomalyco/opencode"

**script/publish.ts/**
- `publish.ts` →  (74 ln)  [typescript]
  → Imports: @opencode-ai/script
  → imports: @opencode-ai/script, bun, url

**script/raw-changelog.ts/**
- `raw-changelog.ts` →  (261 ln)  [typescript]
  → Imports: bun
  → imports: bun, util

**script/stats.ts/**
- `stats.ts` →  (225 ln)  [typescript]
  → async function sendToPostHog(event: string, properties: Record<string, any>) {

**script/sync-zed.ts/**
- `sync-zed.ts` →  (130 ln)  [typescript]
  → Imports: bun
  → imports: bun, os, path

**script/version.ts/**
- `version.ts` →  (36 ln)  [typescript]
  → Imports: @opencode-ai/script
  → imports: @opencode-ai/script, bun

**sdks/vscode/.gitignore/**
- `.gitignore` →  (1 ln)  [file]

**sdks/vscode/README.md/**
- `README.md` →  (34 ln)  [docs]

**sdks/vscode/esbuild.js/**
- `esbuild.js` →  (54 ln)  [javascript]
  → @type {import('esbuild').Plugin} /

---
Generated: 2026-05-04 02:45:04Z
