Root: script/tsconfig.json

ASCII Tree:

├── [export] TSConfig for script package

Description
- TS configuration for the script package used during build/typechecking.

Data Flow
- Used by TypeScript tooling when compiling the script.

Side Effects
- None.
